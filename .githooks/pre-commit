#!/usr/bin/env sh
set -eu

echo "[pre-commit] running make fmt"
make fmt

echo "[pre-commit] running make ci"
make ci

echo "[pre-commit] running make cov"
make cov

echo "[pre-commit] checks passed"
